{
  "name": "babel-plugin-with-typescript-starter",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "babel src --extensions .ts --out-dir dist --delete-dir-on-start",
    "test": "jest",
    "lint": "tsc -p src && tsc -p tests && prettier --loglevel warn --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "main": "dist/index.js",
  "keywords": [
    "babel-plugin"
  ],
  "devDependencies": {
    "@babel/cli": "^7.7.4",
    "@babel/core": "^7.7.4",
    "@babel/preset-env": "^7.7.4",
    "@babel/preset-typescript": "^7.7.4",
    "@types/jest": "^24.0.23",
    "@types/node": "^10.0.0",
    "jest": "^24.9.0",
    "prettier": "^1.19.1",
    "typescript": "^3.7.3"
  }
}
